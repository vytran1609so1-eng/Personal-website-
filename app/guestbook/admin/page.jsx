import AdminPage from "@/components/AdminPage";

export const metadata = {
  title: "Moderate notes",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <AdminPage />;
}
